{
  "name": "readtils-extension",
  "version": "1.0.0",
  "description": "Reading utilities - Dark mode toggle and page to markdown conversion",
  "scripts": {
    "build": "node scripts/build.js",
    "package:chrome": "cd dist/chrome && zip -r ../readtils-chrome.zip .",
    "package:firefox": "cd dist/firefox && zip -r ../readtils-firefox.zip ."
  },
  "devDependencies": {
    "fs-extra": "^11.0.0"
  },
  "license": "MIT"
}
